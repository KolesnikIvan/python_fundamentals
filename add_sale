"""Реализовать простую систему хранения данных о суммах продаж булочной.
Скрипт с интерфейсом командной строки для записи данных.
При записи передавать из командной строки значение суммы продаж."""
import sys

if len(sys.argv[1]) == 1:
    sale = input('Input sales sum')
else:
    sale = sys.argv[1]

with open('sales.txt', 'a', encoding='utf-8') as f:
    f.write(sale + '\n')
