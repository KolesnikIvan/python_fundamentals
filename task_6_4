""" Реализовать парсинг данных из файлов.
Получить отдельно фамилию, имя, отчество и название каждого хобби.
Преобразовать в контейнерный тип (список, кортеж, множество, словарь).
Обосновать выбор типа.
Какие могут возникнуть проблемы.
В словаре должны храниться данные, полученные в результате парсинга."""
import sys


# _, file_fio, file_hobby = sys.argv  # вариант для запуска из командной строки
file_fio = 'fio.csv'
file_hobby = 'hobby.csv'
result = dict()
# результат определю словарем
# потому, что нужна же связь имени с соответствующим(и) хобби,
# а неизменяемые типы порядок не поддерживают
# и я не представляю, как в них эту связь обеспечить
with open(file_fio, 'r', encoding='ANSI') as fio:
    with open(file_hobby, 'r', encoding='ANSI') as fhb:
        # у меня возникли проблемы с кодировкой;
        # ANSI не первая, которую пробовал, но певая, показавшая кириллические символы
        # получилось, что слова разделены точками с запятой
        # в другой кодировке могут оказаться другие разделители
        l_fio = fio.readline()
        l_fhb = fhb.readline()
        while l_fio:
            surname, name, patronym = (l_fio.split(';'))
            hb_t = (l_fhb.split(';'))
            # если разделитель - не семиколон, тут тоже будут проблемы
            # возможно, нужна какая-то доп. процедура, которая установит вид разделителя
            result[l_fio.replace(';', ' ')] = \
                {'Name': name, 'Surname': surname, 'Patronymic': patronym, 'Hobby': hb_t}
            # совпадение фио маловероятно, но защиты от такого события нет
            # здесь может быть потеряна информация
            if l_fhb and not l_fio:
                sys.exit(1)  # прекращаем просмотр, если хоббий больше, чем людей
            l_fio = fio.readline()
            l_fhb = fhb.readline()

print(result)
